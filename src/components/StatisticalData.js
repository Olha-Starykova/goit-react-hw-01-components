import React from 'react';



const StatisticalData = ({label, percentage}) => (
     <>
         <span class="label">{label}</span>
         <span class="percentage">{percentage}</span>
    </>
);



export default StatisticalData;